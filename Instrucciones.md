# SQLite_ExternalFunctions
TO TEST:

1.Clone the Repository (git clone <-urlproject->)

2.Create a database file in SQLite and change the path of the SQLiteconnection(in the file Program.cs) and then compile 
